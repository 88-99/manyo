<h1>【管理者用】ユーザ一覧</h1>
<table>
<thead>
  <tr>
    <th>ユーザID：</th><th>ユーザネーム：</th><th>タスク数：</th><th>admin </th>
  </tr>
</thead>
<tbody>
  <% @users.each do |user| %>
    <tr>
      <td><%= user.id %></td>
      <td><%= user.name %></td>
      <td><%= user.tasks.size %></td>
      <td><%= user.admin %></td>
      <td><%= link_to '詳細', admin_user_path(user.id), class: "btn btn-primary" %></td>
      <td><%= link_to "ユーザ編集", edit_admin_user_path(user.id), class: "btn btn-success abc" %></td>
      <td><%= link_to "ユーザ削除", admin_user_path(user.id), method: :delete, class: "btn btn-danger" %></td>
    </tr>
  <% end %>
</tbody>
</table>
<%= link_to "ユーザ作成画面へ", new_admin_user_path, class: "btn btn-light" %>
